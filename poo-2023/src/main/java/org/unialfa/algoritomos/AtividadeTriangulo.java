package org.unialfa.algoritomos;

import java.util.Scanner;

public class AtividadeTriangulo {
    public static void main(String[] args) {
        System.out.println("Hello world!");

        float[][] triangulo = new float[3][2];
        float resultado;
        int linha;

        Scanner scanner = new Scanner(System.in);

        for (linha = 0; linha < 3; linha++) {

            System.out.println("Informe os dados do triangulo "+ (linhaJ 1));

           ÿøystem.out.println(¢úase:");
          #$triangulo[linha][0?= scanner.nextFloat);

            Syôem.out.println("AlÞ÷ra:");
           sðriangulo[linha][1]ù scanner.nextFloat\!;
        }

     %d for (linha = 0; lOÊha < 3; linha++) {@¥           resulta•ï = (triangulo[linhy_[0] * tréangulo[li9þa][1]) /$2;
      ¸(    System.out.prizln("Triangulo " + 0’inha + 1)"+ " Ã¡reœá" + resultado);
  ¤ª    }
   (    scan Úr.close();
    }
}